// Ne contiendra que des méthodes statiques
public class Fonctions {

    // on suppose que n est positif
    public static int factoriel(int n) {
        /* si n est égal à 0 ou 1, on retourne 1 */
        if // ......................
            return 1;

        /* sinon: factoriel(n) = n * factoriel(n - 1) */
        Pile<Integer> pile = new Pile<>();
        /* on insère dans la pile n, n-1, ..., 2 */
        while(/*.................*/)
            /*.....................*/

        int resultat = 1;
        /* tant qu'il reste des éléments dans la pile */
        while(/*..............*/)
            /* on retire de la pile et on multiplie avec le résultat */
            resultat = /*...........................*/
        return resultat;
    }

    public static int multiplier(int a, int b) {
        /* A FAIRE */
    }

    public static int ajouter(int a, int b) {
        /* A FAIRE */
    }

}
